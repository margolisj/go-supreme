  $('#cart-controls form, #cctrl form').replaceWith( $.nano($(\"script#cart-controls-remove\").html(), {size_id: 59765}) );\n\n  var style = $('.selected').data('style-id');\n  var h = {\"59765\":1,\"cookie\":\"1 item--59765,21347\"};\n\n  var price = +$('.price').text().replace(/[^0-9\\.]/g,'');\n\n    var pure_cart = \"\"\n    var current_total = pure_cart['total'] ? +pure_cart['total'].replace(/[^0-9]\\./g, '') : 0;\n\n    if (isNaN(current_total))\n      current_total = 0;\n\n    current_total = current_total + price;\n\n    h['total'] = \"$\" + current_total;\n    $.cookie('pure_cart', JSON.stringify(h), {expires:1, path: '/'});\n\n  $(document).trigger(\"page:load\");\n  $(\"body\").trigger(\"cart:add\");\n\n  ga_track('Add to Cart Attempt', 'mp_only');\n